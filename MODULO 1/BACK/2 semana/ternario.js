// ternario eh um if mais simples
//saldo
const saldo = 1;
const unidade = (saldo ===1 ? "real" : "reais");
console.log(`Ele tem ${saldo} ${unidade}`);


// ou
//const saldo = 1;
//console.log(`Ele tem ${saldo} ${saldo ===1 ? "real" : "reais"}`);